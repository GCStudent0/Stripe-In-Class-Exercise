<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container my-4">
        <h2 class="text-center">{{title}}</h2>
        <div class="row d-flex justify-content-center" style="margin-top: 46px;">
            {{#each allCourses}}
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="card shadow-lg border-0">
                    <img src="{{this.image}}" class="card-img-top" style="height: 140px; object-fit: cover;"
                        alt="Course Image">
                    <div class="card-body text-center p-2" style="min-height: 220px;">
                        <h5 class="card-title mb-1">
                            <a href="#" class="text-decoration-none text-dark">{{this.title}}</a>
                        </h5>

                        <p class="card-text short-content text-muted mb-1"
                            style="display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                            {{this.description}}
                        </p>


                        <p class="card-text full-text collapse text-muted d-none">
                            {{this.description}}
                        </p>

                        <p class="card-text"><strong>Price:</strong> <span class="text-primary">${{this.price}}</span>
                        </p>

                        <div class="d-flex justify-content-center">
                            <button class="btn btn-outline-secondary btn-sm readMoreBtn mx-4"
                                data-bs-toggle="collapse">Read More</button>
                            <button class="btn btn-info btn-sm buyCourseBtn" data-course-id="{{this._id}}">Buy
                                Course</button>
                        </div>
                    </div>
                </div>
            </div>

            {{/each}}
        </div>
    </div>

    <div class="exploreBtn text-center">
        <button class="btn btn-primary my-6">Explore More Courses</button>
    </div>
    </div>
    <script src="/javascripts/script.js"></script>
    <script src="/javascripts/stripe.js"></script>
</body>
</html>