<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{title}}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container my-4">
    <h2 class="text-center">{{title}}</h2>
    
    <!-- Bulk order form -->
    <form action="/orders/checkout" method="POST">
      <div class="row d-flex justify-content-center" style="margin-top: 46px;">
        {{#each courses}}
          <div class="col-lg-3 col-md-6 mb-4">
            <div class="card shadow-lg border-0">
              <img src="{{this.image}}" class="card-img-top" alt="Course Image" style="height: 140px; object-fit: cover;">
              <div class="card-body text-center p-2" style="min-height: 220px;">
                <h5 class="card-title mb-1">
                  <a href="#" class="text-decoration-none text-dark">{{this.title}}</a>
                </h5>
                <p class="card-text text-muted mb-1" style="display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                  {{this.description}}
                </p>
                <p class="card-text"><strong>Price:</strong> <span class="text-primary">${{this.price}}</span></p>
                
                <!-- Quantity input for bulk order -->
                <div class="mb-2">
                  <label for="qty-{{this._id}}" class="form-label">Quantity:</label>
                  <input type="number" id="qty-{{this._id}}" name="quantities[{{this._id}}]" class="form-control" min="0" value="0">
                </div>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
      <div class="text-center">
        <button type="submit" class="btn btn-info btn-lg">Order Now</button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>